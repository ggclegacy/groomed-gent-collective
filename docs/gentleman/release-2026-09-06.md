# September 6 integrated release

This release combines the Gentleman OS phases and Living Instrument spatial direction with the unpublished Product Brain work from the existing local checkout. It is based on GitHub main at ce9203d and preserves the existing ambassador flows and Vercel configuration.

The two overlapping edits were resolved by retaining both PostgreSQL migration and product-brain validation scripts, and using the updated evidence-oriented Product Studio description. Source HTML research snapshots retain their original bytes for provenance verification.

Validation before publication: 173 tests passed, application lint passed, and TypeScript checks passed. Production deployment uses the existing GitHub-to-Vercel integration.

Account activation and database migrations remain separate operational steps. The Clerk/Neon account adapter stays gated by its required configuration; no credentials or live personal records are included. Mapbox and model-backed flows require their documented provider settings. Blank environment templates document configuration names.
