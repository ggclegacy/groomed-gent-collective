# Preparation only. These values do not activate routes in this checkpoint.
GGC_COMMERCE_ENABLED=false
GGC_COMMERCE_ENVIRONMENT=local
SHOPIFY_AUTH_MODE=client_credentials
SHOPIFY_SAME_ORGANIZATION_CONFIRMED=false
SHOPIFY_SHOP_DOMAIN=
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=
COMMERCE_WORKER_SECRET=
# Reuse the existing account environment, never create a second identity database:
# GGC_ACCOUNT_PROVIDER=clerk-neon
# DATABASE_URL=
# GGC_OWNER_ID=
# CLERK_SECRET_KEY=
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# Migration secret belongs only in the migration runner:
# GGC_MIGRATION_DATABASE_URL=
# API version is pinned in lib/commerce/model.ts to 2026-07.
